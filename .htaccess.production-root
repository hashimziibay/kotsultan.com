# Redirect all traffic into CodeIgniter's public/ folder.
# Use this ONLY if the domain document root is the project root (not /public).
# Prefer setting StackCP document root to /public instead of using this file.

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
